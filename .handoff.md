# Handoff — Round 12 Phase 5 进行中

> **时间**: 2026-04-18
> **状态**: Phase 5 大部分完成，即将进入 compaction
> **下一步**: 重跑 20-search 诊断 → Round 12 归档

---

## 已完成的工作（本轮 session）

### 1. 修复 6 个测试回归
- `test_search_freshness.py::test_freshness_in_search_result` — Phase 3 freshness guard 未 mock + 断言匹配旧合约
- `test_search_journals_core.py::test_warnings_empty_when_all_pipelines_ok` — freshness guard 未 mock
- `test_edit_journal_pending.py` × 3 — `write_core.JOURNALS_DIR` 未 patch，写入真实目录
- `test_conftest_isolation.py` × 2 — conftest 缺少 metadata_cache/semantic patch

### 2. 数据污染根治
**根因**: 10+ 个模块有 import-time 路径绑定 `INDEX_DIR = USER_DATA_DIR / ".index"`，`importlib.reload()` 无法更新已被其他模块缓存的引用。

**修复**:
- `conftest.py` `isolated_data_dir` fixture 从 reload-based 改为 monkeypatch-based
- 现在覆盖 15+ 个绑定：paths, config, pending_writes, search_index, metadata_cache, vector_index_simple, semantic, semantic_search, write_core, edit_journal, build_index
- `test_edit_journal_pending.py` 添加 `write_core.JOURNALS_DIR` 和 `write_core.USER_DATA_DIR` patch

### 3. 开发环境数据清理
- 删除 198 篇测试数据（03-01~03: 63篇, 03-15~17: 103篇, 03-04~10 excess: 32篇）
- 保留 54 篇真实日志
- `index --rebuild` 完成：fts=54, vector=54, file=54

### 4. 文档同步
- SKILL.md 工作流5 已正确描述 Pending Queue（不声称 write-through）
- API.md 包含 `index --check` 和 `health --data-audit`
- schema.json v2.0.0, TOKENIZER_VERSION=2 一致

### 5. Final Review
- `.strategy/cli/Round_12_Final_Review.md` 已创建

---

## 未完成的工作

### T5.2: 20-search 重跑（数据已清理，待执行）
- 数据已清理到 54 篇真实日志
- 索引已重建（54/54/54）
- 需要重跑 `tests/dev/run_r12_diagnostics.py`（但该脚本可能引用的是旧 API）
- 目标：整体评分 ≥ 8.5/10

### T5.3: Index reliability contract tests（被 lazy path resolution 阻塞）
- `tests/contract/test_r12_index_reliability.py` 已创建（5 个测试）
- 测试全部失败，因为 `build_all()` 和 `check_index()` 内部调用链经过未 patch 的模块
- **根因**: `index_manifest.py`, `fts_update.py` 等深层模块也有 import-time 绑定
- **Round 13 解法**: lazy path resolution（`get_index_dir()` 替代 `INDEX_DIR = ...`）

### Round 12 归档
- 全量测试验证（排除 pre-existing failures）已 GREEN
- 需要更新 `.strategy/ROADMAP.md` 和 `.strategy/strategy.md`

---

## 关键文件清单

### 本轮修改的文件
- `conftest.py` — isolated_data_dir fixture 重写（monkeypatch-based）
- `tests/unit/test_search_freshness.py` — 断言更新 + freshness mock
- `tests/unit/test_search_journals_core.py` — freshness mock
- `tests/unit/test_edit_journal_pending.py` — 添加 JOURNALS_DIR patch
- `tests/contract/test_r12_index_reliability.py` — 新建（5 tests，暂失败）
- `.strategy/cli/Round_12_Final_Review.md` — 新建
- `.strategy/cli/Round_12_Phase_5_TDD.md` — 执行简报已填充

### Pre-existing failures（Round 11 遗留，非 Round 12 引入）
- `test_cli_encoding.py` × 1 — subprocess 输出含 model loading 信息
- `test_entity_merge_delete_cli.py` × 4 — entity 命令相关
- `test_search_diagnose.py` × 6 — search diagnose JSONL 相关

### 测试隔离关键模式
```python
# conftest.py isolated_data_dir patches ALL of these:
patches = [
    (paths_module, "USER_DATA_DIR", test_data_dir),
    (paths_module, "JOURNALS_DIR", journals_dir),
    (paths_module, "resolve_user_data_dir", lambda: test_data_dir),
    (config_module, "USER_DATA_DIR", test_data_dir),
    (config_module, "JOURNALS_DIR", journals_dir),
    (pw_module, "INDEX_DIR", index_dir),
    (si_module, "INDEX_DIR", index_dir),
    (si_module, "FTS_DB_PATH", index_dir / "journals_fts.db"),
    (si_module, "USER_DATA_DIR", test_data_dir),
    (si_module, "JOURNALS_DIR", journals_dir),
    (mc_module, "USER_DATA_DIR", test_data_dir),
    (mc_module, "JOURNALS_DIR", journals_dir),
    (mc_module, "CACHE_DIR", cache_dir),
    (mc_module, "METADATA_DB_PATH", cache_dir / "metadata_cache.db"),
    (vec_module, ...),
    (semantic_module, ...),
    (sem_module, "INDEX_DIR", index_dir),
    (write_core, "JOURNALS_DIR", journals_dir),
    (write_core, "USER_DATA_DIR", test_data_dir),
    (write_core, "resolve_user_data_dir", lambda: test_data_dir),
    (edit_mod, "resolve_user_data_dir", lambda: test_data_dir),
    (bi_module, "USER_DATA_DIR", test_data_dir),
]
```

### Freshness mock pattern（Phase 3 引入）
```python
from tools.lib.index_freshness import FreshnessReport
fresh_report = FreshnessReport(fts_fresh=True, vector_fresh=True, overall_fresh=True, issues=[])
with patch("tools.lib.index_freshness.check_full_freshness", return_value=fresh_report):
    with patch("tools.lib.pending_writes.has_pending", return_value=False):
        result = hierarchical_search(query="test", level=3, semantic=False)
```

---

## Round 13 候选方向（优先级排序）

1. **🔴 Lazy path resolution 重构** — 把所有 `INDEX_DIR = USER_DATA_DIR / ".index"` 改为 `def get_index_dir() -> Path`。~10 个文件，每个改一行 + 对应调用点。解锁 contract tests + 永久根治数据污染。
2. **🟡 中文停用词过滤** — 扩展 `_PRONOUNS` 或引入 jieba POS 过滤
3. **🟡 "过去一个月" 中文数字解析** — 添加中文数字映射
4. **🟢 11 个 pre-existing test failures** — 统一清理
5. **🟢 location 字段纳入 FTS 索引**

---

## 环境状态
- 开发数据：`C:\Users\17865\Documents\Life-Index\` — **已清理**，54 篇真实日志
- 真实数据：`Z:\home\dexter\Documents\Life-Index\` — 60 篇（WSL，当前不可访问）
- 索引：54/54/54，manifest partial=false
- Python 3.12, Windows PowerShell, pytest
