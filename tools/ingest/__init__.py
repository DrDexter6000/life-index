# tools/ingest — import provider internal package
#
# Named ``ingest`` (not ``import``) because ``import`` is a Python keyword.
# The public CLI surface is ``life-index import ...`` (registered in tools/__main__.py).
